
const fs = require('fs');
let userData = JSON.parse(fs.readFileSync('Storage/userData.json', 'utf8'));
exports.info = "Rolls a built-in slot machine which gives you a chance to win."
    exports.run = async (message, args, client, ops) => {
        
    }
